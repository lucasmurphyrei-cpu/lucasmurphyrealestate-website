# Templates

This directory is reserved for any content templates used during profile generation (e.g., markdown section templates, prompt templates).

**Note:** HTML templates and the Neighborhood Fit Quiz UI are built in the website project, not here. This project handles data collection, content generation, and JSON export. The website project consumes `output/json/` for rendering.
